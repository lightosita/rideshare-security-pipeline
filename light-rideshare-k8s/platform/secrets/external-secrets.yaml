#1. RIDER SERVICE
#    AWS SM path: light/rider-service
#    Keys: DATABASE_URL, JWT_SECRET, REDIS_HOST, REDIS_PORT, REDIS_PASSWORD,
#          GOOGLE_CLIENT_ID, GOOGLE_CLIENT_SECRET
# -----------------------------------------------------------------------------

apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: rider-service-secrets
  namespace: rideshare-app
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: aws-secrets-manager
    kind: ClusterSecretStore
  target:
    name: rider-service-secrets
    creationPolicy: Owner
  data:
    - secretKey: DATABASE_URL
      remoteRef:
        key: light/rider-service
        property: DATABASE_URL
    - secretKey: JWT_SECRET
      remoteRef:
        key: light/rider-service
        property: JWT_SECRET
    - secretKey: REDIS_HOST
      remoteRef:
        key: light/rider-service
        property: REDIS_HOST
    - secretKey: REDIS_PORT
      remoteRef:
        key: light/rider-service
        property: REDIS_PORT
    - secretKey: REDIS_PASSWORD
      remoteRef:
        key: light/rider-service
        property: REDIS_PASSWORD
    - secretKey: GOOGLE_CLIENT_ID
      remoteRef:
        key: light/rider-service
        property: GOOGLE_CLIENT_ID
    - secretKey: GOOGLE_CLIENT_SECRET
      remoteRef:
        key: light/rider-service
        property: GOOGLE_CLIENT_SECRET

---

# 2. DRIVER SERVICE

apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: driver-service-secrets
  namespace: rideshare-app
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: aws-secrets-manager
    kind: ClusterSecretStore
  target:
    name: driver-service-secrets
    creationPolicy: Owner
  data:
    - secretKey: DATABASE_URL
      remoteRef:
        key: light/driver-service
        property: DATABASE_URL
    - secretKey: JWT_SECRET
      remoteRef:
        key: light/driver-service
        property: JWT_SECRET
    - secretKey: REDIS_HOST
      remoteRef:
        key: light/driver-service
        property: REDIS_HOST
    - secretKey: REDIS_PORT
      remoteRef:
        key: light/driver-service
        property: REDIS_PORT
    - secretKey: REDIS_PASSWORD
      remoteRef:
        key: light/driver-service
        property: REDIS_PASSWORD

---

# 3. TRIP SERVICE

apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: trip-service-secrets
  namespace: rideshare-app
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: aws-secrets-manager
    kind: ClusterSecretStore
  target:
    name: trip-service-secrets
    creationPolicy: Owner
  data:
    - secretKey: DATABASE_URL
      remoteRef:
        key: light/trip-service
        property: DATABASE_URL
    - secretKey: REDIS_URL
      remoteRef:
        key: light/trip-service
        property: REDIS_URL

---

# 4. MATCHING SERVICE

apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: matching-service-secrets
  namespace: rideshare-app
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: aws-secrets-manager
    kind: ClusterSecretStore
  target:
    name: matching-service-secrets
    creationPolicy: Owner
  data:
    - secretKey: DATABASE_URL
      remoteRef:
        key: light/matching-service
        property: DATABASE_URL
    - secretKey: REDIS_URL
      remoteRef:
        key: light/matching-service
        property: REDIS_URL

---
# 5. EMAIL SERVICE

apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: email-service-secrets
  namespace: rideshare-app
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: aws-secrets-manager
    kind: ClusterSecretStore
  target:
    name: email-service-secrets
    creationPolicy: Owner
  data:
    - secretKey: AZURE_EMAIL_CONNECTION_STRING
      remoteRef:
        key: rideshare/email-service
        property: AZURE_EMAIL_CONNECTION_STRING
