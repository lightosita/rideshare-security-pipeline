apiVersion: v1
kind: Service
metadata:
    name: trip-service
    namespace: rideshare-app
    labels:
      app: trip-service
spec:
    selector:
      app: trip-service
    ports:
      - port: 3005
        targetPort: 3005
        protocol: TCP