apiVersion: apps/v1
kind: Deployment
metadata:
    name: rider-service
    namespace: rideshare-app
    labels:
      app: rider-service
spec:
    replicas: 2
    selector:
      matchLabels:
        app: rider-service
    template:
      metadata:
        labels:
          app: rider-service
      spec:
        affinity:
          podAntiAffinity:
            preferredDuringSchedulingIgnoredDuringExecution:
              - weight: 100
                podAffinityTerm:
                  labelSelector:
                    matchExpressions:
                      - key: app
                        operator: In
                        values:
                          - rider-service
                  topologyKey: topology.kubernetes.io/zone
        containers:
          - name: rider-service
            image: 221693237976.dkr.ecr.us-east-1.amazonaws.com/rideshare-rider-service:1.3
            ports:
              - containerPort: 3001
            envFrom:
              - configMapRef:
                  name: rider-service-config
              - secretRef:
                  name: rider-service-secrets
            resources:
              requests:
                cpu: 100m
                memory: 512Mi
              limits:
                cpu: 250m
                memory: 512Mi
            readinessProbe:
              httpGet:
                path: /api/v1/riders/health
                port: 3001
              initialDelaySeconds: 10
              periodSeconds: 5
            livenessProbe:
              httpGet:
                path: /api/v1/riders/health
                port: 3001
              initialDelaySeconds: 30
              periodSeconds: 10