apiVersion: v1
kind: Service
metadata:
    name: rider-service
    namespace: rideshare-app
    labels:
      app: rider-service
spec:
    selector:
      app: rider-service
    ports:
      - port: 3001
        targetPort: 3001
        protocol: TCP